name: Activity Filters
on:
  workflow dispatch:
  pull_request:
    types: [synchronize]

jobs:
  any-old-job-demo:
    runs-on: ubuntu-latest


    steps:
      - name: Any old step
        run: echo "helloooo"



