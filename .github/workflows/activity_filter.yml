name: Activity Filters
on:
  workflow_dispatch:
  pull_request:
    types: [opened, synchronize]
   #segmenting different workflows depending on teams
  push:
    branches:
    - 'dev*'
    - main

jobs:
  any-old-job-demo:
    runs-on: ubuntu-latest


    steps:
      - name: Any old step
        run: echo "helloooo"




